<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.toothscheduleproject">

<!--  알림에 필요한 권한  -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

<!--  구글맵 권한  -->
    <uses-feature android:required="true" android:glEsVersion="0x00020000"/>
    <permission android:name="com.example.toothscheduleproject.permission.MAPS_RECEIVE"
        android:label="signature"></permission>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>
    <meta-data android:name = "com.google.android.gms.version"
        android:value = "@integer/google_play_services_version"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToothScheduleProject">

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SignUp">

        </activity>

        <!--    sub액티비티 등록    -->
        <activity android:name=".Notification"/>
        <activity android:name=".Statistic"/>
        <activity android:name=".InfoBoard"/>
        <activity android:name=".Survey"/>
        <activity android:name=".SurveyResult"/>
        <activity android:name=".SurveyResultBad"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".Schedule"/>
        <activity android:name=".ScheduleDetail"/>
        <activity android:name=".MyPage"/>


        <meta-data android:name="com.google.android.maps.v2.API_KEY" android:value="AIzaSyCMxXCKcm0yX8D-VVC8PR2XFGspiF-D_I4"/>
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>

        <!--    리시버 추가    -->
        <receiver android:name=".AlertReceiver"/>
    </application>

</manifest>